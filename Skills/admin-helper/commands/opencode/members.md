---
description: List or search guild members. Usage: /discord.admin.members [guild_id|search query].
---

Load `admin-helper`. If `$ARGUMENTS` looks like a search query, use `members-search` (no privileged intent). If they want a full roster, use `members` / `members-report` and explain Server Members Intent on 403. Prefer `--json`. Read-only.
