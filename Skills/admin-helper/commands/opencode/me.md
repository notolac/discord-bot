---
description: Smoke-test the bot token (GET /users/@me).
---

Load `admin-helper`. Run `./Skills/admin-helper/scripts/admin_helper --json me` (add `--env-file bots/<bot>/.env` if needed). Report the bot username and id. Never print the token.
