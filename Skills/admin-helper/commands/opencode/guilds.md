---
description: List guilds the bot can see (GET /users/@me/guilds).
---

Load `admin-helper`. Run `./Skills/admin-helper/scripts/admin_helper --json guilds`. Summarize id, name, and approximate_member_count. Use `$ARGUMENTS` as an optional `--env-file` path.
