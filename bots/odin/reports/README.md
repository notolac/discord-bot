# reports/

Script and audit outputs for this bot (JSONL / Markdown). **Gitignored** except this file and
`.gitkeep` — reports may contain Discord user IDs.

Retention: delete files older than 90 days unless a task says otherwise.
